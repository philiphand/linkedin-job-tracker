import React from "react";
import styled from "styled-components";

// Display a table of all search history
// Calculate average search result for each skill and put the skill with the highest average on top
// And descending by average search results

export const History: React.FC = () => {
    return (
        <div>

        </div>
    );
}

